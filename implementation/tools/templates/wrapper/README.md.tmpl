# {{APP_NAME}}

Generated {{DATE}} by {{AUTHOR}}.

## Run

```bash
python main.py
```

Edit `COMMAND` in `main.py` to set the script or command to wrap.

## Key Bindings

| Key | Action |
|-----|--------|
| `↑` / `k` | Scroll output up |
| `↓` / `j` | Scroll output down |
| `PgUp` / `PgDn` | Page scroll |
| `a` | Re-enable auto-scroll |
| `r` | Restart the wrapped command |
| `q` | Quit |
